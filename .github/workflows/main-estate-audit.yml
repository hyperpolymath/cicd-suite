# This workflow is managed by gh actions-lock.

name: Central Estate CI/CD Audit

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_call:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  estate-audit:
    runs-on: ubuntu-latest
    steps:
      # Remote-form refs to THIS repo's actions at a FULL COMMIT SHA, with
      # this workflow DE-ONBOARDED from actions.lock. The empirical cross-repo
      # matrix (all witnessed 2026-09-17, runs 35277504987 / 35277227942 /
      # 35278067796 / 35279718644 / 35280642055 / 35280317253):
      #   $/actions/*            -> resolves at the RUNNING commit = the
      #                             CALLER's commit cross-repo: 0-job death.
      #   ./actions/* + lock     -> "local path actions not supported": 0-job.
      #   ./actions/* de-onboard -> jobs spawn, but ./ resolves against the
      #                             CALLER's repo: "Can't find action.yml".
      #   remote @main/tag + lock (onboarded) -> 0-job startup_failure
      #                             cross-repo (creation-time enforcement under
      #                             the CALLER's jurisdiction; silent).
      #   remote @SHA + de-onboarded -> BOTH components proven: creation
      #                             passes de-onboarded (35279718644 spawned),
      #                             prepare downloads remote SHA composites
      #                             under the caller's sha_pinning_required
      #                             (same run: deed/k9@SHA fetched fine).
      # The bare-by-SHA checkout fetches the CALLING repo cross-repo, so the
      # 26 gates audit the caller's estate — the design intent. Trade-offs,
      # disclosed: gate IMPLEMENTATIONS are pinned to the SHA above (repin
      # when actions/** change; the onboarded code-hygiene-self-test.yml —
      # $/ form, full lockfile enforcement — covers composite changes), and
      # this workflow sits in the gh-actions-lock local-path/remote-self
      # coverage gap ("1 workflow skipped", tolerated not enforced). Revisit
      # when the Technical Preview supports $/ across workflow_call. Revisit when
      # gh-actions-lock supports local-path actions or $/ across calls.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      
      - name: Required Files Gate
        uses: hyperpolymath/cicd-suite/actions/required-files-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Code Hygiene Gate
        uses: hyperpolymath/cicd-suite/actions/code-hygiene-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Manifest Validation Gate
        uses: hyperpolymath/cicd-suite/actions/manifest-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Idris2 ABI Purity Gate
        uses: hyperpolymath/cicd-suite/actions/idris2-abi-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Zig Hexadeca API Gate
        uses: hyperpolymath/cicd-suite/actions/zig-hexadeca-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Contractile Validation Gate
        uses: hyperpolymath/cicd-suite/actions/contractile-validation-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Recipes Set Validation Gate
        uses: hyperpolymath/cicd-suite/actions/recipes-set-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Affirmation Document Gate
        uses: hyperpolymath/cicd-suite/actions/affirmation-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Academic Referencing Gate
        uses: hyperpolymath/cicd-suite/actions/referencing-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Semantic Audit Gate
        uses: hyperpolymath/cicd-suite/actions/semantic-audit-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: SPDX License Gate
        uses: hyperpolymath/cicd-suite/actions/spdx-license-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Proof Runner Gate
        uses: hyperpolymath/cicd-suite/actions/proof-runner-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: PRAT Testing Gate
        uses: hyperpolymath/cicd-suite/actions/prat-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Panic Attack & Pons Gate
        uses: hyperpolymath/cicd-suite/actions/custom-tools-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: WWW & Well-Known Compliance Gate
        uses: hyperpolymath/cicd-suite/actions/www-compliance-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: BoJ Cartridge Validation Gate
        uses: hyperpolymath/cicd-suite/actions/boj-cartridge-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Formatting Validation Gate
        uses: hyperpolymath/cicd-suite/actions/formatting-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Accreditations & Badges Gate
        uses: hyperpolymath/cicd-suite/actions/badges-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Metrics Extraction Gate
        uses: hyperpolymath/cicd-suite/actions/metrics-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Linguist & Banned Languages Gate
        uses: hyperpolymath/cicd-suite/actions/linguist-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Test & Benchmarks Dashboard Gate
        uses: hyperpolymath/cicd-suite/actions/tests-benches-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Hosting & Site Status Gate
        uses: hyperpolymath/cicd-suite/actions/hosting-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Git-Sea Analytics Gate
        uses: hyperpolymath/cicd-suite/actions/gitsea-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Trust & Humans Validation Gate
        uses: hyperpolymath/cicd-suite/actions/trust-humans-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Are We UnAPI Gate (Secret Scanning)
        uses: hyperpolymath/cicd-suite/actions/secrets-check@0da816c05ae7486671d863e07935f93981c0c2d5
      - name: Reasonably Good Token Validation Gate
        uses: hyperpolymath/cicd-suite/actions/vaulted-tokens-check@0da816c05ae7486671d863e07935f93981c0c2d5